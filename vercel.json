{
    "builds": [{
        "src": "ivh_inventario/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "ivh_inventario/wsgi.py"
        }
    ]
}